<link rel="stylesheet" href="{{ 'styles.css' | asset_url }}">
<script type="text/javascript" src="{{ 'script.js'| asset_url }}" async></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js" async></script>

<div>
  <div class='skin-analysis__container'>
    <div class="analysis-image" id="analysis-image">
      <video id="cameraFeed" class="camera-feed"></video>
      <button id="captureImage">Capture</button>
      <button id="captureButton">Capture</button>
      <button id="closeButton">X</button>
      <progress id="progressBar" value="0" max="100" style="width:250px;"></progress>
      <p id="uploadPercentage"></p>
    </div>
    {% comment %} <div class="analysis-image" id="analysis-image">

      <img id="uploadedImage" src="#" width="auto" height="auto" alt="Uploaded Image" style="display: none;">
      <form id="upload_form" enctype="multipart/form-data" method="post">
        <input type="file" name="file" id="file" class="inputfile" onchange="uploadFile()" />
        <label for="file" id="fileLabel">Choose a file</label>
        <progress id="progressBar" value="0" max="100" style="width:250px;"></progress>
        <p id="uploadPercentage"></p>
      </form>
    </div> {% endcomment %}
    <div class="analysis-text" id="analysis-text">
      <div class="text1" id="text1">Our AI analyzes over 10+ skin metrics to offer personalized product suggestions for
        your unique beauty journey.</div>
      <div class="text2" id="text2">
        <h3>Combination skin</h3>
        <p>Combination skin is a common skin type characterized by having both oily and dry areas on the face. Typically,
          the T-zone (forehead, nose, and chin) tends to be oilier, while the cheeks may be drier. This duality can make
          skincare routines a bit tricky because what works for one area might not be suitable for another.</p>
        <p>Common issues</p>
        <div id="issues-list">
          <ul>
            <li>Clogged pores</li>
            <li>Flakiness</li>
            <li>Tightness</li>
            <li>Occasional sensitivity</li>
            <li>Dehydration</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="recommendations recommendations--hidden" id="recommendations-container">
    <h2>Recommended Products</h2>
    <div class="" id="recommedation-list"></div>
  </div>
</div> 
{% schema %}
{
"name": "Skin analysis",
"target": "section"
}
{% endschema %}